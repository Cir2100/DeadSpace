<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable name="startViewModel"
            type="com.example.deadspace.view.ui.main.StartViewModel"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/start"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="a"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <Button
            android:id="@+id/button_search"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="25dp"
            android:layout_marginLeft="200dp"
            android:text="button_search"
            app:layout_constraintTop_toBottomOf="@+id/message"
            tools:layout_editor_absoluteX="161dp"
            android:onClick="@{() -> startViewModel.onSearch()}"/>

        <Button
            android:id="@+id/button_add"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="25dp"
            android:layout_marginLeft="200dp"
            android:text="button_add"
            app:layout_constraintTop_toBottomOf="@+id/button_search"
            tools:layout_editor_absoluteX="161dp"
            android:onClick="@{() -> startViewModel.addPair()}"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>